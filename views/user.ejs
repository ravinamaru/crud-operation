<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All user data</title>
    <style>
        table, td, th{
            border: 2px solid black;
        }
    </style>
</head>
<body>
    <table>
        <tr>
            <th>Id</th>
            <th>_Name</th>
            <th>_Email</th>
            <th>Edit</th>
            <th>Delete</th>
        </tr>
        <% for(user of data) {%>
            <tr>
                <td><%= user.id %></td>
                <td><%= user.name %></td>
                <td><%= user.email %></td>
                <td>
                    <form method="get" action="/user/<%= user.id %>/edit">
                       <button>Edit</button>
                    </form>
                </td>
                <td>
                    <form method="get" action="/user/<%= user.id %>/delete">
                       <button>Delete</button>
                    </form>
                </td>
            </tr>
        <% } %>
        <tr><a href="/user/new">Add New user</a></tr>
    </table>
</body>
</html>